---
const { title, href, text, tags = [], media = 'placeholder' } = Astro.props;
---
<a class="project-card rich-project-card" href={href}>
  <div class="project-thumb" data-media={media}></div>
  <div>
    <h3>{title}</h3>
    <p>{text}</p>
    <div class="tag-row">
      {tags.map((tag) => <span>{tag}</span>)}
    </div>
  </div>
</a>
