---
import PortfolioLayout from '../../../layouts/PortfolioLayout.astro';
import { biometriaNeonatalPageContent as content } from '../../../data/pages/biometriaNeonatal';
---

<PortfolioLayout title={content.meta.title} description={content.meta.description}>
  <section
    class="project-detail-hero project-hero-ai project-hero-image"
    style={`--project-hero-image: url('${content.hero.image.src}')`}
  >
    <div>
      <p class="eyebrow">{content.hero.category}</p>
      <h1>{content.hero.title}</h1>
      <p>{content.hero.description}</p>
      <div class="tag-row">
        {content.hero.tags.map((tag) => <span>{tag}</span>)}
      </div>
    </div>
  </section>

  <article class="content-section robotnik-page biometria-page">
    <section class="project-intro-grid">
      <div class="project-body longform">
        <p class="lead">{content.summary}</p>

        <h2>{content.context.title}</h2>
        {content.context.paragraphs.map((paragraph) => <p>{paragraph}</p>)}

        <h2>{content.participation.title}</h2>
        {content.participation.paragraphs.map((paragraph) => <p>{paragraph}</p>)}
      </div>

      <aside class="project-aside">
        <strong>Projeto</strong><span>Biometria Neonatal</span>
        <strong>Instituição</strong><span>UTFPR - Campus Pato Branco</span>
        <strong>Tipo</strong><span>Pesquisa / TCC</span>
        <strong>Foco</strong><span>Inferência de IA em Raspberry Pi e ARM Linux</span>
      </aside>
    </section>

    <section class="project-section">
      <article class="contact-card development-card">
        <h2>{content.development.title}</h2>
        <p>{content.development.description}</p>
      </article>
    </section>

    <section class="project-section project-two-columns">
      <div>
        <div class="section-heading compact-heading">
          <h2>Tecnologias e conceitos</h2>
        </div>

        <div class="skills-cloud compact-skills">
          {content.technologies.map((technology) => <span>{technology}</span>)}
        </div>
      </div>

      <div class="project-body longform">
        <h2>Aprendizados</h2>
        <ul>
          {content.learnings.map((learning) => <li>{learning}</li>)}
        </ul>
      </div>
    </section>

    <section class="project-section external-links-section">
      <div class="section-heading compact-heading">
        <h2>Links acadêmicos</h2>
      </div>

      <div class="contact-card compact-actions">
        {content.externalLinks.map((link) => (
          <a class="btn btn-primary" href={link.href} target="_blank" rel="noopener noreferrer" referrerpolicy="no-referrer">{link.label}</a>
        ))}
        <a
          class="btn"
          href="https://www.researchgate.net/profile/Guilherme-Rodrigues-65"
          target="_blank"
          rel="noopener noreferrer"
          referrerpolicy="no-referrer"
        >
          ResearchGate
        </a>
      </div>
    </section>
  </article>
</PortfolioLayout>
